package com.driveleaders.persistence

object CustomNameMapper {
	// make configuration / add a common list of mappings
	//map from singular to plural
	val CUSTOM_MAPPINGS = Map(
		"entity" -> "entities"
	)
}